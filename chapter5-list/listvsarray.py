#list vs array
# in array we can only store single datatype elements
# in list we can store any datatype elements
#python has array module can be used to use array
#array module has better performance than list, but we wont use it


#list vs string
#list are mutable where as strings are immutable