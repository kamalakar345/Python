#tuple data structure
#tuple can store any data type
#most important tuples are immutables ,once tuple is created you cannot add any update
#data inside tuple

example = ('one', 'two', 'three', 'one')
#no append, no insert, no pop, no remove
# days = ('monday', 'tuesday')
#tuples are faster than lists

#methods in tuples
#count , index
#len function
#slicing
print(example.count('one'))
print(example.index('two'))
print(len(example))
